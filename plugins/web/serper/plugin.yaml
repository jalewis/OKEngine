name: web-serper
version: 1.0.0
description: "Serper (google.serper.dev) — Google SERP web search. Requires SERPER_API_KEY (free tier 2.5k queries at https://serper.dev). OKEngine overlay plugin (okengine#190) — adds a Serper backend Hermes doesn't ship, joining web.backend: rotate."
author: OKEngine
kind: backend
provides_web_providers:
  - serper
