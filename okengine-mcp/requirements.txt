# Pinned for reproducible builds (see docs/supply-chain.md to bump).
# Verify `mcp` against okengine-mcp/server.py + write_server.py when changing it.
mcp==1.28.1
PyYAML==6.0.2
uvicorn==0.34.0
fastapi==0.138.0          # review-only governed HTTP surface; keep aligned with Cockpit
