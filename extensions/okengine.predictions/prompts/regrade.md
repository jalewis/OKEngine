Re-grade open predictions against new evidence. The select_regrade_batch.py digest above lists open predictions + new source pages and the REQUIRED `evidence:` entry fields. FIRST response MUST be a tool call. For each source that bears on a claim: append a one-line prose entry to the body `## Evidence log` (append_to_section), and append a structured record to the frontmatter `evidence:` list — carrying the fields the digest specifies (`date`, `direction`, `confidence_before`, `confidence_after`, `source`, `note`) — while setting top-level `confidence:` to the new value, via update_entity (send the COMPLETE evidence list). The cockpit reads those fields for the trajectory sparkline + reinforces/contradicts tally + confidence-move column, so a bare prose string starves them. No-op is correct when no new source bears. LOCAL-ONLY (no web tools).
