Re-grade open predictions against new evidence. The select_regrade_batch.py digest above lists exact dependency-matched prediction/source pairs, any pending deterministic confidence recommendation, and the REQUIRED `evidence:` entry fields. FIRST response MUST be a tool call. For each source that bears on a claim: append a one-line prose entry to the body `## Evidence log` (append_to_section), and append a structured record to the frontmatter `evidence:` list — carrying the fields the digest specifies (`date`, `direction`, `confidence_before`, `confidence_after`, `source`, `note`) — via update_entity (send the COMPLETE evidence list). The cockpit reads those fields for the trajectory sparkline + reinforces/contradicts tally + confidence-move column, so a bare prose string starves them. No-op is correct when no new source bears. LOCAL-ONLY (no web tools).

CONFIDENCE DISPOSITION (#212): for newly assessed evidence with no recommendation, set `confidence_before` and `confidence_after` to the current top-level confidence and leave top-level `confidence` unchanged. Equality is the explicit pending marker; the deterministic recommender will join the record to event scores and calculate a clamped delta. When the digest carries a recommendation, apply its suggested value to top-level `confidence` and the named evidence record's `confidence_after`. If the suggested value equals the current confidence, append `[recommender-accepted]` to that record's `note` so the no-change recommendation closes. Choose a different value only when the record's `note` states the concrete deviation reason; if that different value still equals `confidence_before`, encode it as `[recommender-deviation: concrete reason]`. If the recommender abstains because scored inputs are unavailable, confidence MUST NOT rise unless a later evidence record documents a neutral/contradictory skeptic pass or an explicit falsification search that found none.

SCORING DISCIPLINE (okengine#213 — from a measured 12:1 reinforces bias vs 2:1 real outcomes, and a refuted prediction's own postmortem): score `direction` against the prediction's SPECIFIC RESOLUTION CRITERIA (its `## What would refute this` / satisfaction criteria), never against the topic's importance. Evidence that the subject is serious, widespread, or much-discussed but says nothing about the SPECIFIC claimed outcome is `neutral`. Evidence the threat is serious BUT CONTAINED/mitigated at scale WEAKENS an imminent-incident claim — score it `contradicts` or `partial`, not `reinforces` (the postmortem case: fleet-scale containment evidence was scored reinforces at 0.72 on a prediction that was then refuted). When in doubt between reinforces and neutral, choose neutral — the falsification lane exists to argue the other side.
