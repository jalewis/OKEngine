Forecasting base rates. The select_base_rates.py digest above lists resolved predictions. FIRST response MUST be a tool call. Cluster them into recurring CLASSES and maintain dashboards/base-rates (create or update_entity via okengine-write) with each class: historical resolution rate, n, and example [[predictions/...]]. Conservative - omit a class with too few points. Per $WIKI_PATH/CLAUDE.md. LOCAL-ONLY (no web tools).
