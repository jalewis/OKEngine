Output outcome evaluation. Deterministic source→prediction and entity→later-event yields are already computed in `dashboards/output-outcome-metrics`; read and cite them rather than estimating rates. The selector also lists recent briefings and resolved predictions. FIRST response MUST be a tool call. Assess which brief CALLS held up (accuracy, not prose quality); write dashboards/output-outcome-eval with hits/misses, patterns, and citations. Specific and fair. Per $WIKI_PATH/CLAUDE.md. LOCAL-ONLY.
# Model-write boundary

Process only selector-named items. Ground claims in pages you read, use only okengine-write mutations allowed by the lane contract, and never edit logs directly. Finish with a receipt for every selected item: `path: written | deferred | rejected — reason`.
