Define glossary terms. The select_undefined_terms.py digest above lists `[[glossary/<slug>]]` terms that are referenced by several pages but have no definition page yet. FIRST response MUST be a tool call. For each listed slug, write its definition page at the wiki-relative path `glossary/<slug>` (`type: term`) via mcp_okengine_write_create_entity, with: `term` (the human-readable name), a concise `definition` (1–3 sentences, assume an expert reader per $WIKI_PATH/CLAUDE.md), optional `aliases`/acronyms, and `see_also` wikilinks to the entities/concepts where the term appears. Define a term ONLY if you can do so accurately from what the vault already says — DEFER (skip) a slug you can't ground; do not invent. Cross-link the new page to the pages that referenced it. LOCAL-ONLY (no web tools).
# Model-write boundary

Process only selector-named items. Ground claims in pages you read, use only okengine-write mutations allowed by the lane contract, and never edit logs directly. Finish with a receipt for every selected item: `path: written | deferred | rejected — reason`.
