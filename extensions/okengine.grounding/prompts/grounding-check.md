Semantic grounding check. The select_grounding_check.py digest above lists grounded entities + their cited sources. FIRST response MUST be a tool call. For EACH entity: read it AND each cited source page (mcp_okengine_get_page), then via the okengine-write path append a `## Grounding check` section (append_to_section) classifying each substantive claim as supported / unsupported / not-found-in-source (cite the source), and update_entity to set `grounding_checked: <today>` (send the complete value of any field you change). Conservative — flag only CLEAR unsupported/contradicted claims, never phrasing or omissions. A clean pass (all supported) is a valid, recorded result. Per $WIKI_PATH/CLAUDE.md. LOCAL-ONLY (no web tools).
