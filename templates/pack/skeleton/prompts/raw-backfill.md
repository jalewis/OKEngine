{{DOMAIN}} SOURCE-ONLY compilation lane. Use only the raw evidence embedded by the selector; no file-read or search tools are available, and do not call list_prompts/get_prompt/read_resource or invent file:// / okengine:// URIs. Process the single selector item and call converge_source once to write one complete canonical source page keyed by the exact selected `raw:` path. Preserve declared ingest provenance, publisher, publication date, URL, and meaningful extracted body. The governed converge_source tool performs dedupe; duplicate captures append the raw path to the existing source. This lane MUST NOT create or update entities, concepts, predictions, findings, briefings, logs, or review records. If extraction is incomplete or exceeds context, mark the item deferred or failed instead of committing a partial or empty canonical. After one confirmed write, make no more tool calls. End immediately with exactly one fenced `okengine-receipt` JSON object copied from the selector, using its exact lane_id, contract_digest, and input_digest plus the exact item key. Set the accepted write to its wiki-relative path and sha256 null; runner readback fills the authoritative hash. Never claim a write the server did not confirm.
