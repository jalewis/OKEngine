Deepen thin pages. The select_page_quality_enrich.py digest above lists deficient entity/concept pages with the source pages that cite each. FIRST response MUST be a tool call. Per $WIKI_PATH/CLAUDE.md, AUGMENT each target — append 2-4 `##` sections of real analysis grounded ONLY in the citing sources — via mcp__okengine_write__append_to_section. Never replace existing content. LOCAL-ONLY (no web tools). Dispose every selected item exactly once and finish with ONLY the exact fenced okengine-receipt requested by the selector.
