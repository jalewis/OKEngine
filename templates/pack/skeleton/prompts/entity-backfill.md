{{DOMAIN}} grounded entity synthesis. Consume only accepted canonical source pages, using the source evidence embedded by the selector. No file-read, search, or listing tools are available; do not request other pages. Select only the single highest-value durable subject supported by that source and call converge_entity once with its complete grounded entity representation and the exact canonical source slug. Classify the subject, not its proximity to malicious activity: a company, publisher, victim, researcher, product, technique, method, campaign, exploit, vulnerability, malware or ransomware family, tool, payload, lure, or named variant is not a threat actor merely because reporting mentions it. Use `type: actor` only when the submitted body explicitly defines the named subject as an adversarial group, intrusion set, criminal organization, operation, or operator; an `actor_type`, source grade, evidence count, backlink count, or news count is not entity-class proof. Use `publisher` or `identity` for organizations when those schema types are available. If the schema has no supported non-actor type, skip/defer the subject instead of coercing it into `actor`. Generic phrases such as "threat actor" or "AI agents" are never entities and must be skipped. The governed writer resolves IDs and aliases, refuses duplicates, and is responsible for resolving `sources` relationship paths, so do not perform manual dedupe reconnaissance. Treat every writer result other than an explicit error or rejection as success, including created, updated, accepted, review, unchanged, or kept-owner-field results. After a successful result, make no more tool calls. Retry only an explicit error or rejection and never exceed three total converge_entity calls. Never create sources, concepts, predictions, findings, briefings, logs, or review records. Then emit only the exact fenced okengine-receipt copied from the selector; leave accepted write sha256 values null for runner readback. Use skipped with a reason when the source contains no entity-worthy evidence.
