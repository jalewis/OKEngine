{{DOMAIN}} concept backfill. The select_concept_candidates.py digest above lists missing concept slugs (wikilinked but no page). FIRST response MUST call converge_concept. Create concept pages (a cross-cutting pattern / segment / cluster) synthesized from the citing sources. If a slug is really an entity name, it is an ENTITY not a concept — skip it. Write only through converge_concept. LOCAL-ONLY (no web tools).
