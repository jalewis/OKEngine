diff --git a/cron/scheduler.py b/cron/scheduler.py
index 898e5cb..d5a1995 100644
--- a/cron/scheduler.py
+++ b/cron/scheduler.py
@@ -3027,9 +3027,11 @@ def run_job(
         # ticks short-circuit on already-connected servers inside
         # register_mcp_servers(). Non-fatal on failure: a broken MCP server
         # shouldn't kill an otherwise-working cron job. See #4219.
+        _cron_enabled_toolsets = _resolve_cron_enabled_toolsets(job, _cfg)
+        _require_explicit_cron_tool_surface(job, _cron_enabled_toolsets)
+        _mcp_tools = []
         try:
-            from tools.mcp_tool import discover_mcp_tools
-            _mcp_tools = discover_mcp_tools()
+            _mcp_tools = _discover_cron_mcp_tools(_cron_enabled_toolsets)
             if _mcp_tools:
                 logger.info(
                     "Job '%s': %d MCP tool(s) available",
@@ -3040,6 +3042,7 @@ def run_job(
                 "Job '%s': MCP initialization failed (non-fatal): %s",
                 job_id, _mcp_exc,
             )
+        _validate_cron_mcp_surface(job, _cron_enabled_toolsets, _mcp_tools)
-
+
         agent = AIAgent(
             model=model,
@@ -3060,7 +3063,7 @@ def run_job(
             providers_order=pr.get("order"),
             provider_sort=pr.get("sort"),
             openrouter_min_coding_score=(_cfg.get("openrouter") or {}).get("min_coding_score"),
-            enabled_toolsets=_resolve_cron_enabled_toolsets(job, _cfg),
+            enabled_toolsets=_cron_enabled_toolsets,
             disabled_toolsets=_apply_failure_path_toolset_guard(
                 _resolve_cron_disabled_toolsets(_cfg), _script_failed
             ),
