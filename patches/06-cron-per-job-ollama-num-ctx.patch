diff --git a/agent/agent_init.py b/agent/agent_init.py
index 251db3e15..43c57c635 100644
--- a/agent/agent_init.py
+++ b/agent/agent_init.py
@@ -174,6 +174,7 @@ def init_agent(
     command: str = None,
     args: list[str] | None = None,
     model: str = "",
+    ollama_num_ctx: int | None = None,  # okengine#151: per-job/profile ctx override
     max_iterations: int = 90,  # Default tool-calling iterations (shared with subagents)
     tool_delay: float = 1.0,
     enabled_toolsets: List[str] = None,
@@ -1786,7 +1787,12 @@ def init_agent(
     # budget is respected even when GGUF metadata advertises a larger window.
     agent._ollama_num_ctx: int | None = None
     _ollama_num_ctx_override = None
-    if isinstance(_model_cfg, dict):
+    # okengine#151: a per-job/per-lane value (e.g. resolved from a model profile and threaded
+    # through run_job -> AIAgent) takes precedence over config.yaml model.ollama_num_ctx, and is
+    # honored the same way (an explicit cap — the auto-detect/cap branches below are skipped).
+    if ollama_num_ctx is not None:
+        _ollama_num_ctx_override = ollama_num_ctx
+    elif isinstance(_model_cfg, dict):
         _ollama_num_ctx_override = _model_cfg.get("ollama_num_ctx")
     if _ollama_num_ctx_override is not None:
         try:
diff --git a/cron/scheduler.py b/cron/scheduler.py
index 4c764bd13..a9732d1a1 100644
--- a/cron/scheduler.py
+++ b/cron/scheduler.py
@@ -2799,6 +2799,7 @@ def run_job(job: dict) -> tuple[bool, str, str, Optional[str]]:
 
         agent = AIAgent(
             model=model,
+            ollama_num_ctx=job.get("ollama_num_ctx"),  # okengine#151: per-lane ctx (model profile)
             api_key=runtime.get("api_key"),
             base_url=runtime.get("base_url"),
             provider=runtime.get("provider"),
diff --git a/run_agent.py b/run_agent.py
index 497197f76..9f99d79b6 100644
--- a/run_agent.py
+++ b/run_agent.py
@@ -434,6 +434,7 @@ class AIAgent:
         command: str = None,
         args: list[str] | None = None,
         model: str = "",
+        ollama_num_ctx: int | None = None,  # okengine#151: per-job/profile ctx override
         max_iterations: int = 90,  # Default tool-calling iterations (shared with subagents)
         tool_delay: float = 1.0,
         enabled_toolsets: List[str] = None,
@@ -509,6 +510,7 @@ class AIAgent:
             command=command,
             args=args,
             model=model,
+            ollama_num_ctx=ollama_num_ctx,
             max_iterations=max_iterations,
             tool_delay=tool_delay,
             enabled_toolsets=enabled_toolsets,
