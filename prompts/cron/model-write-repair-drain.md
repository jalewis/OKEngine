Historical model-write repair. Read `.okengine/model-write-repair-batch.json`. Process every selected action exactly once and only through the lane MCP writer. Immediately before each write, re-read `canonical_path` and defer the item if its sha256 no longer equals `selected_sha256`; pass `selected_sha256` as `expected_sha256` to update_entity or converge_entity so the server refuses a concurrent change. Never overwrite a concurrently changed page. Path roots are explicit: `canonical_path` is under the vault `wiki/` directory, while every `declared_raw` path is relative to the VAULT ROOT (for example `raw/x.md` means `/opt/vault/raw/x.md`, NEVER `/opt/vault/wiki/raw/x.md`). Use `read_file` for those explicit local paths and `get_page` for canonical vault pages; NEVER invent a `file://` URI for `read_resource`, which accepts only URIs returned verbatim by `list_resources`. For `recompile-from-declared-raw`, read only those exact declared raw files, then converge a complete page without inventing facts, links, or fields; defer when `declared_raw` is empty or unavailable. For `quarantine-for-review`, call flag_for_review with the audit reason; do not fabricate replacement content. Honor expected sha/version preconditions and defer changed pages. Your final response must be ONLY one fenced `okengine-receipt` JSON object copied from `receipt_template`, with every angle-bracket placeholder replaced. Do not emit prose, Markdown tables, YAML, `=====` markers, or a plain code fence. Keep the runner-owned identity and item keys byte-for-byte. An accepted page recompile must list every canonical write with its wiki-relative path and current sha256; the runner replaces a stale model-copied hash with immediate authoritative read-back. An accepted quarantine-for-review has no target-page write and keeps `writes: []`; the runner verifies its review-queue effect. Remove placeholder writes for every other non-accepted item and give the reason. Never create helper files or scripts to calculate hashes.
